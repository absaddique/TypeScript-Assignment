function make_album(artist_name:string,title:string, tracks: any){
    // const object = {`${artist_name},
    // ${title},
    // ${tracks}`}
    if(tracks= undefined){
        console.log(`Artist Name : ${artist_name};
Title       : ${title},
Tracks      : ${tracks}`);
}else{
    console.log(`Artist Name : ${artist_name};
Title       : ${title},
Tracks      : ${tracks}`);
}
}
make_album("Talha","Farhan",2);
make_album("Haseeb","Hadier",5);